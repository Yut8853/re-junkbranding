<section class="section final-cta" data-transition-to="final-cta" aria-labelledby="contact-title">
  <div class="final-cta__inner">
    <p class="final-cta__label" data-reveal>Contact</p>
    <h2 class="final-cta__title" id="contact-title" data-reveal data-reveal-delay="0.06">
      伝わらないままに、<br />
      しておかない。
    </h2>
    <p class="final-cta__lead" data-reveal data-reveal-delay="0.12">
      Webサイトの見せ方、世界観、動きの設計まで。<br />
      まずは今の課題をお聞かせください。
    </p>
    <a
      class="final-cta__button"
      href="mailto:hello@junkbranding.com?subject=Web%E5%88%B6%E4%BD%9C%E3%81%AE%E7%9B%B8%E8%AB%87"
      data-reveal
      data-reveal-delay="0.18"
    >
      <span>相談する</span>
      <svg viewBox="0 0 24 24" aria-hidden="true">
        <path d="M5 12h13" />
        <path d="m13 6 6 6-6 6" />
      </svg>
    </a>
  </div>
</section>

<style lang="scss">
  .final-cta {
    position: relative;
    min-height: 100svh;
    display: grid;
    place-items: center;
    padding-inline: var(--gutter);
    text-align: center;
    overflow: hidden;
  }

  .final-cta::before {
    content: '';
    position: absolute;
    inset: 18% 12%;
    z-index: 0;
    pointer-events: none;
    background:
      linear-gradient(90deg, transparent, rgba(188, 239, 255, 0.34), transparent) 50% 0 / 100% 1px no-repeat,
      linear-gradient(90deg, transparent, rgba(255, 255, 255, 0.22), transparent) 50% 100% / 100% 1px no-repeat;
    opacity: 0.72;
  }

  .final-cta__inner {
    position: relative;
    z-index: 1;
    width: min(100%, 980px);
    margin-inline: auto;
  }

  .final-cta__label {
    margin: 0 0 clamp(1.2rem, 3vw, 2rem);
    font-size: 0.72rem;
    line-height: 1;
    letter-spacing: 0.42em;
    text-transform: uppercase;
    color: rgba(188, 239, 255, 0.78);
  }

  .final-cta__title {
    margin: 0;
    font-family: var(--font-serif);
    font-size: clamp(2.35rem, 7vw, 6.8rem);
    font-weight: 600;
    line-height: 1.18;
    letter-spacing: 0.02em;
    color: #fff;
    text-shadow:
      0 0 2.6rem rgba(188, 239, 255, 0.22),
      0 1.4rem 4.5rem rgba(0, 0, 0, 0.42);
  }

  .final-cta__lead {
    margin: clamp(1.4rem, 3.5vw, 2.4rem) auto 0;
    max-width: 42rem;
    font-size: clamp(0.96rem, 1.35vw, 1.18rem);
    line-height: 2.05;
    color: rgba(226, 233, 244, 0.76);
  }

  .final-cta__button {
    display: inline-flex;
    align-items: center;
    justify-content: center;
    gap: 1rem;
    min-height: 4.6rem;
    margin-top: clamp(2.2rem, 5vw, 3.8rem);
    padding: 0.05rem 3rem 0;
    border: 1px solid #fff;
    border-radius: 999px;
    background: #fff;
    color: #111;
    font-size: 1.05rem;
    font-weight: 600;
    letter-spacing: 0.18em;
    text-decoration: none;
    transition:
      border-color 0.28s ease,
      background 0.28s ease,
      color 0.28s ease,
      transform 0.28s ease,
      box-shadow 0.28s ease;
  }

  .final-cta__button svg {
    width: 1.3rem;
    height: 1.3rem;
    fill: none;
    stroke: currentColor;
    stroke-linecap: round;
    stroke-linejoin: round;
    stroke-width: 1.7;
    transition: transform 0.28s ease;
  }

  .final-cta__button:hover {
    transform: translateY(-2px);
    border-color: #fff;
    background: #111;
    color: #fff;
    box-shadow:
      0 1rem 3rem rgba(0, 0, 0, 0.28),
      0 0 2rem rgba(188, 239, 255, 0.16);
  }

  .final-cta__button:hover svg {
    transform: translateX(0.22rem);
  }

  @media (max-width: 640px) {
    .final-cta::before {
      inset: 22% 0;
    }

    .final-cta__lead br {
      display: none;
    }
  }

  @media (prefers-reduced-motion: reduce) {
    .final-cta__button,
    .final-cta__button svg {
      transition: none;
    }
  }
</style>
